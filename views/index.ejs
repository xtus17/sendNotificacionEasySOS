<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta
      name="description"
      content="EasySOS App - Plataforma de Alerta Ciudadana"
    />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link rel="icon" href="/assets/Logo.png" type="image/x-icon" />

    <style>
      :root {
        --primary-color: #ff6b35;
        --secondary-color: #004e89;
        --warning-color: #ffc107;
      }

      body {
        background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
        min-height: 100vh;
        font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
      }

      .main-container {
        max-width: 700px;
        margin: 0 auto;
        padding: 30px 20px;
      }

      .header-card {
        background: white;
        border-radius: 20px;
        padding: 30px;
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
        margin-bottom: 30px;
        text-align: center;
      }

      .header-card img {
        width: 100px;
        height: 100px;
        object-fit: contain;
        margin-bottom: 15px;
      }

      .header-card h1 {
        color: var(--secondary-color);
        font-weight: 700;
        font-size: 2.5rem;
        margin: 0;
      }

      .header-card .subtitle {
        color: #6c757d;
        font-size: 1rem;
        margin-top: 10px;
      }

      .alert-cards {
        display: grid;
        grid-template-columns: 1fr;
        gap: 20px;
        margin-bottom: 30px;
      }

      .alert-card {
        background: white;
        border-radius: 15px;
        padding: 25px;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        transition: all 0.3s ease;
        text-decoration: none;
        color: inherit;
        display: block;
        border-left: 5px solid var(--warning-color);
      }

      .alert-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 8px 20px rgba(0, 0, 0, 0.15);
      }

      .alert-card-icon {
        font-size: 2.5rem;
        margin-bottom: 10px;
      }

      .alert-card-title {
        font-weight: 700;
        font-size: 1.3rem;
        color: var(--secondary-color);
        margin-bottom: 8px;
      }

      .alert-card-description {
        color: #6c757d;
        font-size: 0.95rem;
        margin: 0;
      }

      .form-card {
        background: white;
        border-radius: 15px;
        padding: 30px;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
      }

      .form-card h2 {
        color: var(--secondary-color);
        font-weight: 700;
        margin-bottom: 20px;
        font-size: 1.5rem;
      }

      .form-label {
        font-weight: 600;
        color: #333;
        margin-bottom: 8px;
      }

      .form-control {
        border: 2px solid #e0e0e0;
        border-radius: 10px;
        padding: 15px;
        transition: all 0.3s ease;
      }

      .form-control:focus {
        border-color: var(--primary-color);
        box-shadow: 0 0 0 0.2rem rgba(255, 107, 53, 0.25);
      }

      .btn-send {
        background: var(--primary-color);
        border: none;
        color: white;
        padding: 15px 40px;
        font-size: 1.1rem;
        font-weight: 600;
        border-radius: 10px;
        transition: all 0.3s ease;
        width: 100%;
      }

      .btn-send:hover {
        background: #e55a2b;
        transform: translateY(-2px);
        box-shadow: 0 6px 12px rgba(255, 107, 53, 0.4);
      }

      @media (max-width: 576px) {
        .header-card h1 {
          font-size: 2rem;
        }
      }
    </style>

    <title>EasySOS App - Plataforma de Alerta Ciudadana</title>
  </head>
  <body>
    <div class="main-container">
      <div class="header-card">
        <a href="https://www.easysos.app">
          <img src="/assets/Logo.png" alt="EasySOS Logo" />
        </a>
        <h1>EasySOS</h1>
        <p class="subtitle">Plataforma de Alerta Ciudadana</p>
      </div>

      <div class="alert-cards">
        <a href="/send-alert" class="alert-card">
          <div class="alert-card-icon">🚨</div>
          <div class="alert-card-title">Persona Desaparecida</div>
          <p class="alert-card-description">
            Reporta y difunde alertas de personas desaparecidas de forma
            inmediata
          </p>
        </a>
      </div>

      <div class="form-card">
        <h2>📢 Enviar Alerta General</h2>

        <form action="/send-notifications" method="post">
          <div class="mb-4">
            <label for="customBody" class="form-label">
              Descripción de la alerta
            </label>
            <input
              type="text"
              class="form-control"
              id="customBody"
              name="customBody"
              minlength="6"
              required
              placeholder="Ej: Robo en Av. Principal esquina Calle 5, sospechoso con camisa roja..."
            />
            <div class="form-text">
              Describe brevemente la situación e incluye ubicación (mínimo 6
              caracteres)
            </div>
          </div>

          <button type="submit" class="btn btn-send">
            Enviar Alerta Ahora
          </button>
        </form>
      </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

    <script>
      // ⭐ LOADER AL ENVIAR ALERTA GENERAL
      const generalForm = document.querySelector(
        'form[action="/send-notifications"]',
      );
      const sendBtn = document.querySelector(".btn-send");

      generalForm.addEventListener("submit", function (e) {
        // Deshabilitar botón
        sendBtn.disabled = true;

        // Agregar spinner y cambiar texto
        sendBtn.innerHTML = `
      <span class="spinner-border spinner-border-sm me-2" role="status" aria-hidden="true"></span>
      Enviando...
    `;

        // Cambiar estilo del botón
        sendBtn.style.opacity = "0.8";
        sendBtn.style.cursor = "not-allowed";
      });
    </script>
  </body>
</html>
